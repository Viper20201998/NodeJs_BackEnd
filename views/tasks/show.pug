doctype html
html
    head
        title Curso-Backend
    body
        h1= tasks.description
        p= tasks.createdAt
        p
            strong Autor: 
            span=tasks.user.email
        form(action=`/tasks/${tasks.id}?_method=DELETE` method="POST")
            input(type="submit" value="Eliminar")
